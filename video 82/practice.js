console.log("Practice Set");

async function delay() {
    //     return new Promise((resolve, reject) => {
    //         setTimeout(() => {
    //             resolve("Hello World");
    //         }, 2000);
    //     })
    // }

    // function avg(num){
    //     const sum = num.reduce((accumulator, currentValue) => accumulator + currentValue, 0);
    //     const avg = sum/num.length;

    //     return avg;

    // return new Promise((resolve, reject) => {
    //     setTimeout(() => {
    //         resolve(`Resolved after ${n} seconds`)
    //     }, n*1000);
    // })



}

// function interest(p, r, t) {
//     const Interest = (p * r * t) / 100;
//     return Interest;
// }

(async () => {
    // let a = await delay();
    // console.log(a)

    // const arr = [5, 10, 15, 20]
    // console.log(avg([...arr]))

    // let a = prompt("Enter the time in seconds");

    // console.log(await delay(a));

    // let p = prompt("Enter the Principle amount");

    // let r = prompt("Enter the Principle rate");

    // let t = prompt("Enter the Principle time");

    // console.log(interest(p, r, t));

    
})()